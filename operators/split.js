var headCount=4;

var Total=345;

var gst=8;

var gstAmount=(gst/100)*Total;

console.log("Gst Amount:",gstAmount);

var TotalAmount=Total+gstAmount;

console.log("Total Amount",TotalAmount);

var perAmount=TotalAmount/headCount;

console.log("Per head:",perAmount);

